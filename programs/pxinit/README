This is our init program. It does very little except launch a shell on the console.

The intent in Pathetix is to run nothing by default. The organization of the system is Unix-like but it is very much intended for single-user microcomputers.

